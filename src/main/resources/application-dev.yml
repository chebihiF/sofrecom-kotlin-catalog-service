spring:
  datasource:
    url: jdbc:mysql://localhost:3306/CourseCatalogDB
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root
    password:
  jpa:
    database: mysql
    database-platform: org.hibernate.dialect.MySQL5Dialect
    hibernate:
      ddl-auto: update
    generate-ddl: true
    show-sql: true